# 🏗️ flower-bot Architecture Documentation

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                          CLIENT LAYER                                │
├──────────────────────────┬──────────────────────────────────────────┤
│   Telegram Mobile App    │     Telegram Desktop/Web                 │
│   • Bot Commands         │     • Mini App (WebApp)                  │
│   • Inline Keyboards     │     • Photo Gallery                      │
│   • Location Sharing     │     • Shopping Cart                      │
└──────────────────────────┴──────────────────────────────────────────┘
                                     │
                                     │ HTTPS/WebSocket
                                     ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    TELEGRAM BOT API (9.x)                            │
│   • Update routing                                                   │
│   • Webhook/Polling                                                  │
│   • Message delivery                                                 │
│   • Payment processing (TON Stars)                                   │
└─────────────────────────────────────────────────────────────────────┘
                                     │
                                     │ Bot API
                                     ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      APPLICATION LAYER                               │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │              bot.py (Main Application)                        │  │
│  │  • Application builder                                        │  │
│  │  • Handler registration                                       │  │
│  │  • Webhook/polling config                                     │  │
│  │  • Error handling                                             │  │
│  │  • Database initialization                                    │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                              │                                        │
│              ┌───────────────┼───────────────┐                       │
│              │               │               │                       │
│  ┌───────────▼────┐ ┌───────▼─────┐ ┌──────▼──────┐               │
│  │  flowers.py    │ │  orders.py  │ │  admin.py   │               │
│  │  ──────────    │ │  ─────────  │ │  ────────   │               │
│  │  • /start      │ │  • Cart     │ │  • /admin   │               │
│  │  • /recommend  │ │  • Location │ │  • CRUD     │               │
│  │  • /build FSM  │ │  • Payment  │ │  • MinIO    │               │
│  │  • AI rec.     │ │  • Orders   │ │  • Stats    │               │
│  │  • Image gen.  │ │  • Geocode  │ │             │               │
│  └────────────────┘ └─────────────┘ └─────────────┘               │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘
                                     │
          ┌──────────────────────────┼──────────────────────────┐
          │                          │                           │
          ▼                          ▼                           ▼
┌─────────────────┐      ┌──────────────────┐       ┌──────────────────┐
│  DATA LAYER     │      │   EXTERNAL APIs  │       │  STORAGE LAYER   │
│  ──────────     │      │   ─────────────  │       │  ─────────────   │
│                 │      │                  │       │                  │
│  database.py    │      │  • Perplexity    │       │  • MinIO (S3)    │
│  ───────────    │      │    AI Chat API   │       │    Photo storage │
│                 │      │                  │       │                  │
│  ┌───────────┐ │      │  • Stable Diff   │       │  • PostgreSQL    │
│  │   User    │ │      │    Image Gen API │       │    or SQLite     │
│  ├───────────┤ │      │                  │       │    Database      │
│  │   Flower  │ │      │  • Yandex        │       │                  │
│  ├───────────┤ │      │    Geocoder API  │       │                  │
│  │   Order   │ │      │                  │       │                  │
│  └───────────┘ │      │  • Telegram API  │       │                  │
│                 │      │    Payment, etc. │       │                  │
└─────────────────┘      └──────────────────┘       └──────────────────┘
```

---

## Component Flow Diagrams

### 1. User Purchase Flow

```
┌──────┐
│ User │
└──┬───┘
   │ /start
   ▼
┌────────────────┐
│  Main Menu     │
│  • Catalog     │
│  • Recommend   │
│  • Build       │
└───┬────────────┘
    │
    ├─ Option A: Browse Catalog ────────────────────────────┐
    │  1. Click "Каталог"                                    │
    │  2. Mini App opens                                     │
    │  3. Filter by category                                 │
    │  4. Add to cart                                        │
    │  5. Return to bot                                      │
    └────────────────────────────────────────────────────────┘
    │
    ├─ Option B: AI Recommendation ─────────────────────────┐
    │  1. Send "повод:birthday, бюджет:2000"                 │
    │  2. Perplexity API analyzes                            │
    │  3. Bot suggests bouquet                               │
    │  4. Click "Add to cart"                                │
    └────────────────────────────────────────────────────────┘
    │
    └─ Option C: Build Custom ──────────────────────────────┐
       1. /build command                                     │
       2. Select color (FSM state 1)                         │
       3. Select quantity (FSM state 2)                      │
       4. Select add-ons (FSM state 3)                       │
       5. Generate preview (Stable Diffusion/Pillow)         │
       6. Add to cart                                        │
       └─────────────────────────────────────────────────────┘
    │
    │ View cart: /cart
    ▼
┌────────────────┐
│  Shopping Cart │
│  • Item list   │
│  • Total price │
└───┬────────────┘
    │ "Оформить заказ"
    ▼
┌────────────────┐
│ Location Share │
│ (GPS coords)   │
└───┬────────────┘
    │
    ▼
┌────────────────┐
│ Yandex Geocode │
│ (address resol)│
└───┬────────────┘
    │
    ▼
┌────────────────┐
│ Payment Invoice│
│ (TON Stars)    │
└───┬────────────┘
    │ User pays
    ▼
┌────────────────┐
│  Order Created │
│  status: paid  │
└───┬────────────┘
    │
    ▼
┌────────────────┐
│  Confirmation  │
│  "Спасибо!"    │
└────────────────┘
```

---

### 2. Admin Flower Management Flow

```
┌───────┐
│ Admin │
└───┬───┘
    │ /admin (checks ADMIN_IDS)
    ▼
┌─────────────────┐
│  Admin Panel    │
│  • List Flowers │
│  • Add Flower   │
│  • View Orders  │
│  • View Users   │
└────┬────────────┘
     │ "Добавить цветок"
     ▼
┌─────────────────┐
│ FSM State 1:    │
│ Upload Photo    │
└────┬────────────┘
     │ Photo sent
     ▼
┌─────────────────┐
│ MinIO Storage   │
│ • Upload photo  │
│ • Get URL       │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ FSM State 2:    │
│ Enter Name      │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ FSM State 3:    │
│ Enter Desc      │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ FSM State 4:    │
│ Enter Price     │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ FSM State 5:    │
│ Select Category │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Save to DB      │
│ (Flower model)  │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Success Message │
│ Return to panel │
└─────────────────┘
```

---

### 3. Data Flow Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                      Request Flow                             │
└──────────────────────────────────────────────────────────────┘

User Action (Telegram)
        │
        ▼
Telegram Bot API
        │
        ├─── Webhook (Production) ───────┐
        │                                 │
        └─── Polling (Development) ──────┤
                                          │
                                          ▼
                              ┌───────────────────┐
                              │  bot.py           │
                              │  Application      │
                              └─────────┬─────────┘
                                        │
                              ┌─────────▼─────────┐
                              │  Handler Router   │
                              │  (by update type) │
                              └─────────┬─────────┘
                                        │
                    ┌───────────────────┼───────────────────┐
                    │                   │                   │
            ┌───────▼───────┐  ┌───────▼───────┐  ┌───────▼───────┐
            │ Command       │  │ Callback      │  │ Message       │
            │ Handler       │  │ Query Handler │  │ Handler       │
            └───────┬───────┘  └───────┬───────┘  └───────┬───────┘
                    │                  │                   │
                    └──────────────────┼───────────────────┘
                                       │
                            ┌──────────▼──────────┐
                            │  Business Logic     │
                            │  (handlers/*.py)    │
                            └──────────┬──────────┘
                                       │
                ┌──────────────────────┼──────────────────────┐
                │                      │                      │
        ┌───────▼────────┐   ┌────────▼────────┐   ┌────────▼────────┐
        │ Database       │   │ External API    │   │ Storage         │
        │ Operations     │   │ Calls           │   │ Operations      │
        │ (SQLAlchemy)   │   │ (httpx/aiohttp) │   │ (MinIO)         │
        └───────┬────────┘   └────────┬────────┘   └────────┬────────┘
                │                     │                      │
                │                     │                      │
        ┌───────▼────────┐   ┌────────▼────────┐   ┌────────▼────────┐
        │ PostgreSQL/    │   │ • Perplexity    │   │ S3-compatible   │
        │ SQLite         │   │ • Stable Diff   │   │ Object Store    │
        │                │   │ • Yandex        │   │                 │
        └────────────────┘   └─────────────────┘   └─────────────────┘
                │                     │                      │
                └──────────────────────┼──────────────────────┘
                                       │
                            ┌──────────▼──────────┐
                            │  Response Builder   │
                            └──────────┬──────────┘
                                       │
                            ┌──────────▼──────────┐
                            │  Telegram Bot API   │
                            │  (send_message,     │
                            │   send_photo, etc.) │
                            └──────────┬──────────┘
                                       │
                                       ▼
                               User (Telegram)
```

---

## Technology Stack Details

### Backend (Python)

```
┌─────────────────────────────────────────────────────────┐
│                   Python 3.11+                           │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌────────────────┐  ┌────────────────┐               │
│  │   Bot Layer    │  │  Data Layer    │               │
│  ├────────────────┤  ├────────────────┤               │
│  │ python-        │  │ SQLAlchemy     │               │
│  │ telegram-bot   │  │ 2.0.25         │               │
│  │ 21.0           │  │                │               │
│  │                │  │ aiosqlite      │               │
│  │ • Handlers     │  │ 0.19.0         │               │
│  │ • Keyboards    │  │                │               │
│  │ • FSM          │  │ Models:        │               │
│  │ • Webhooks     │  │ • User         │               │
│  └────────────────┘  │ • Flower       │               │
│                      │ • Order        │               │
│  ┌────────────────┐  └────────────────┘               │
│  │  AI/ML Layer   │                                    │
│  ├────────────────┤  ┌────────────────┐               │
│  │ Perplexity API │  │ Storage Layer  │               │
│  │ (via httpx)    │  ├────────────────┤               │
│  │                │  │ MinIO 7.2.0+   │               │
│  │ Stable Diff    │  │ S3-compatible  │               │
│  │ WebUI API      │  │                │               │
│  │ (webuiapi)     │  │ Buckets:       │               │
│  │                │  │ • flowers      │               │
│  │ Pillow 10.0+   │  └────────────────┘               │
│  │ (fallback)     │                                    │
│  └────────────────┘  ┌────────────────┐               │
│                      │  Geo Layer     │               │
│  ┌────────────────┐  ├────────────────┤               │
│  │  Utilities     │  │ yandex-        │               │
│  ├────────────────┤  │ geocoder 3.0+  │               │
│  │ httpx 0.26+    │  │                │               │
│  │ aiohttp 3.9+   │  │ • Reverse      │               │
│  │ python-dotenv  │  │   geocoding    │               │
│  │ pydantic 2.5+  │  │ • Address      │               │
│  └────────────────┘  │   resolution   │               │
│                      └────────────────┘               │
└─────────────────────────────────────────────────────────┘
```

### Frontend (Telegram Mini App)

```
┌─────────────────────────────────────────────────────────┐
│               Telegram Mini App (WebApp)                 │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │               index.html                          │  │
│  │  • Responsive layout                              │  │
│  │  • Photo gallery grid                             │  │
│  │  • Filter interface                               │  │
│  │  • Shopping cart UI                               │  │
│  │  • Checkout form                                  │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │               css/style.css                       │  │
│  │  • Telegram theme integration                     │  │
│  │  • CSS variables for colors                       │  │
│  │  • Responsive breakpoints                         │  │
│  │  • Grid layout (3/2/1 columns)                    │  │
│  │  • Dark/Light mode support                        │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │               js/app.js                           │  │
│  │  • Telegram Web App SDK                           │  │
│  │  • Flower filtering logic                         │  │
│  │  • Cart management (localStorage)                 │  │
│  │  • Category selection                             │  │
│  │  • Haptic feedback                                │  │
│  │  • Theme detection                                │  │
│  │  • MainButton control                             │  │
│  │  • Data posting to bot                            │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## Deployment Architecture

### Development (Docker Compose)

```
┌─────────────────────────────────────────────────────────┐
│                    Docker Network                        │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   bot        │  │  postgres    │  │   minio      │ │
│  │              │  │              │  │              │ │
│  │ Python 3.11  │  │ PostgreSQL   │  │ S3 Storage   │ │
│  │ flower-bot   │◄─┤ 15-alpine    │  │ latest       │ │
│  │              │  │              │  │              │ │
│  │ Port: 8443   │  │ Port: 5432   │  │ Port: 9000   │ │
│  │              │  │              │  │ UI: 9001     │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
│         ▲                                               │
│         │                                               │
│         │ Polling Mode                                  │
│         │                                               │
└─────────┼───────────────────────────────────────────────┘
          │
          │
   ┌──────▼──────┐
   │  Telegram   │
   │  Bot API    │
   └─────────────┘
```

### Production (Railway)

```
┌─────────────────────────────────────────────────────────┐
│                    Railway Platform                      │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │              flower-bot Service                   │  │
│  │  • Auto-deploy from GitHub                        │  │
│  │  • Environment variables                          │  │
│  │  • HTTPS endpoint                                 │  │
│  │  • Auto-scaling                                   │  │
│  └───────────────┬──────────────────────────────────┘  │
│                  │                                      │
│  ┌───────────────▼──────────────────────────────────┐  │
│  │         PostgreSQL Add-on                         │  │
│  │  • Managed database                               │  │
│  │  • Automatic backups                              │  │
│  │  • Connection pooling                             │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
└─────────────────────────────────────────────────────────┘
          │
          │ Webhook Mode (HTTPS)
          │
   ┌──────▼──────┐
   │  Telegram   │
   │  Bot API    │
   └─────────────┘

External Services (from Railway env):
• MinIO (external or S3-compatible)
• Perplexity API
• Stable Diffusion API
• Yandex Geocoder
```

---

## Security Architecture

```
┌─────────────────────────────────────────────────────────┐
│                  Security Layers                         │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  Layer 1: Authentication & Authorization                 │
├─────────────────────────────────────────────────────────┤
│  • Telegram user verification (via Bot API)              │
│  • Admin ID whitelist (ADMIN_IDS env var)                │
│  • No custom authentication (trust Telegram)             │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 2: Input Validation                               │
├─────────────────────────────────────────────────────────┤
│  • Price validation (positive float)                     │
│  • User ID validation (integer)                          │
│  • Pydantic models for data validation                   │
│  • Type hints enforcement                                │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 3: Data Protection                                │
├─────────────────────────────────────────────────────────┤
│  • SQLAlchemy ORM (prevents SQL injection)               │
│  • Parameterized queries only                            │
│  • No raw SQL execution                                  │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 4: Secrets Management                             │
├─────────────────────────────────────────────────────────┤
│  • Environment variables for all secrets                 │
│  • .env.example template (no real secrets)               │
│  • .gitignore excludes .env                              │
│  • No hardcoded credentials                              │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 5: Transport Security                             │
├─────────────────────────────────────────────────────────┤
│  • HTTPS for webhooks (required by Telegram)             │
│  • TLS for database connections (PostgreSQL)             │
│  • Secure API endpoints (HTTPS)                          │
└─────────────────────────────────────────────────────────┘
```

---

## State Management

### FSM (Finite State Machine) States

```
┌─────────────────────────────────────────────────────────┐
│            Bouquet Builder Conversation                  │
└─────────────────────────────────────────────────────────┘

START
  │
  │ /build or "Создать букет"
  ▼
┌────────────────┐
│ SELECT_COLOR   │  States stored in context.user_data
├────────────────┤
│ Data:          │  Example:
│ • bouquet: {}  │  {
│                │    "bouquet": {
│ User selects:  │      "color": "red",
│ 🔴 Red         │      "quantity": 15,
│                │      "addons": ["ribbon"],
│ Next → QUANTITY│      "price": 2700
└────────┬───────┘    }
         │          }
         ▼
┌────────────────┐
│ SELECT_QUANTITY│
├────────────────┤
│ Options:       │
│ • 5 flowers    │
│ • 7 flowers    │
│ • 11 flowers   │
│ • 15 flowers   │
│ • 21 flowers   │
│ • 25 flowers   │
│                │
│ Next → ADDONS  │
└────────┬───────┘
         │
         ▼
┌────────────────┐
│ SELECT_ADDONS  │
├────────────────┤
│ Options:       │
│ • 🎀 Ribbon    │
│ • 📦 Luxury    │
│ • 🧸 Teddy     │
│ • 🍫 Chocolate │
│ • ✅ Done      │
│                │
│ Next → PREVIEW │
└────────┬───────┘
         │
         ▼
┌────────────────┐
│ GENERATE       │
│ PREVIEW        │
├────────────────┤
│ • AI image gen │
│ • Or Pillow    │
│ • Add to cart  │
│                │
│ Next → END     │
└────────────────┘
```

### Shopping Cart State

```
context.user_data['cart'] = [
    {
        "type": "preset",  # or "custom"
        "flower_id": 1,
        "name": "Розы классические",
        "price": 2500.0,
        "quantity": 1
    },
    {
        "type": "custom",
        "color": "red",
        "quantity": 15,
        "addons": ["ribbon"],
        "price": 2700.0
    }
]
```

---

## API Integration Patterns

### 1. Perplexity API (AI Recommendations)

```python
# Request Pattern
async def get_ai_recommendation(occasion, budget, color):
    async with httpx.AsyncClient() as client:
        response = await client.post(
            "https://api.perplexity.ai/chat/completions",
            headers={
                "Authorization": f"Bearer {PERPLEXITY_API_KEY}",
                "Content-Type": "application/json"
            },
            json={
                "model": "llama-3.1-sonar-small-128k-online",
                "messages": [
                    {
                        "role": "system",
                        "content": "You are a flower expert..."
                    },
                    {
                        "role": "user",
                        "content": f"occasion: {occasion}, budget: {budget}"
                    }
                ]
            },
            timeout=30.0
        )
        return response.json()
```

### 2. Stable Diffusion (Image Generation)

```python
# Request Pattern
from webuiapi import WebUIApi

api = WebUIApi(host='localhost', port=7860)

result = api.txt2img(
    prompt="Beautiful bouquet of 15 red roses",
    negative_prompt="ugly, blurry",
    steps=20,
    cfg_scale=7,
    width=512,
    height=512
)

image_base64 = result.images[0]  # Base64 encoded
```

### 3. Yandex Geocoder (Address Resolution)

```python
# Request Pattern
from yandex_geocode import YandexGeocode

geocoder = YandexGeocode(api_key=YANDEX_API_KEY)

# Reverse geocoding (coords → address)
result = geocoder.reverse(
    latitude=55.7558,
    longitude=37.6173
)

address = result['formatted']
# "Россия, Москва, Красная площадь, 1"
```

### 4. MinIO (Photo Storage)

```python
# Request Pattern
from minio import Minio

client = Minio(
    endpoint="minio.example.com:9000",
    access_key="access_key",
    secret_key="secret_key",
    secure=True
)

# Upload
client.put_object(
    bucket_name="flowers",
    object_name="rose_123.jpg",
    data=photo_bytes,
    length=len(photo_bytes),
    content_type="image/jpeg"
)

# Get URL
url = client.presigned_get_object(
    bucket_name="flowers",
    object_name="rose_123.jpg",
    expires=timedelta(days=7)
)
```

---

## Performance Considerations

### Async Operations Flow

```
User Request
    │
    ▼
┌──────────────────────┐
│  Handler (async)     │
│  await operation()   │
└──────────┬───────────┘
           │
           ├─── Database Query (async) ──────┐
           │                                  │
           ├─── API Call (async) ─────────── │
           │                                  │
           └─── File Operation (async) ───── │
                                              │
                    All run concurrently      │
                    via asyncio event loop    │
                                              │
                    ┌─────────────────────────┘
                    │
                    ▼
           Results aggregated
                    │
                    ▼
           Response to user
```

### Database Connection Pooling

```python
# SQLAlchemy async session maker
engine = create_async_engine(DATABASE_URL)
async_session_maker = async_sessionmaker(
    engine,
    class_=AsyncSession,
    expire_on_commit=False
)

# Usage pattern
async with async_session_maker() as session:
    # Session auto-managed
    result = await session.execute(query)
    # Auto-commit on exit
```

---

## Scaling Considerations

### Horizontal Scaling Challenges

```
Current Architecture (Single Instance):
┌────────────────────┐
│     Bot Instance   │
│  • In-memory cart  │
│  • FSM state       │
└────────────────────┘

Problem: State in context.user_data not shared

Solution for Multi-Instance:
┌────────────┐  ┌────────────┐  ┌────────────┐
│ Bot Inst 1 │  │ Bot Inst 2 │  │ Bot Inst 3 │
└──────┬─────┘  └──────┬─────┘  └──────┬─────┘
       │               │               │
       └───────────────┼───────────────┘
                       │
            ┌──────────▼──────────┐
            │   Redis/Memcached   │
            │   Shared State      │
            └─────────────────────┘
```

**Recommendations:**
1. Use Redis for session storage
2. Implement distributed locks for admin operations
3. Use webhook mode with load balancer
4. Sticky sessions or state replication

---

## Error Handling Strategy

```
┌─────────────────────────────────────────────────────────┐
│              Error Handling Hierarchy                    │
└─────────────────────────────────────────────────────────┘

Application Level (bot.py)
    │
    ▼
┌────────────────────────────────────┐
│  Global Error Handler              │
│  • Logs all exceptions             │
│  • Sends user-friendly message     │
│  • No sensitive data exposure      │
└────────────────────────────────────┘

Handler Level (flowers.py, orders.py, admin.py)
    │
    ▼
┌────────────────────────────────────┐
│  Try-Catch Blocks                  │
│  • Specific exception handling     │
│  • Fallback mechanisms             │
│  • User notifications              │
└────────────────────────────────────┘

External API Level
    │
    ▼
┌────────────────────────────────────┐
│  API-Specific Handling             │
│  • Timeout handling                │
│  • Retry logic (optional)          │
│  • Fallback to alternative         │
│    - Stable Diff → Pillow          │
│    - Perplexity → Mock DB query    │
└────────────────────────────────────┘

Database Level
    │
    ▼
┌────────────────────────────────────┐
│  SQLAlchemy Exception Handling     │
│  • Connection errors               │
│  • Integrity errors                │
│  • Rollback on failure             │
└────────────────────────────────────┘
```

---

## Monitoring & Observability (Recommended)

```
Current: Basic logging to stdout

Recommended Stack:

┌────────────────────────────────────┐
│         Application Logs           │
│  (structured logging)              │
└───────────────┬────────────────────┘
                │
                ▼
┌────────────────────────────────────┐
│         Log Aggregation            │
│  (ELK, Loki, or Cloud Logging)     │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│         Metrics Collection         │
│  (Prometheus, StatsD)              │
│  • Request rate                    │
│  • Error rate                      │
│  • Response time                   │
│  • Active users                    │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│         Tracing                    │
│  (Jaeger, Zipkin)                  │
│  • Request flow                    │
│  • External API calls              │
│  • Database queries                │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│         Alerting                   │
│  (PagerDuty, Opsgenie)             │
│  • High error rate                 │
│  • API failures                    │
│  • Database issues                 │
└────────────────────────────────────┘
```

---

## Summary

This architecture demonstrates:
- ✅ Clean separation of concerns (handlers, database, APIs)
- ✅ Async-first design for performance
- ✅ Scalable with minor modifications (Redis for state)
- ✅ Modular integration with external services
- ✅ Production-ready deployment options
- ✅ Security best practices

**Key Strengths:**
1. Modern async Python implementation
2. Comprehensive feature set
3. Well-organized codebase
4. Multiple deployment options
5. Graceful fallbacks for external dependencies

**Areas for Enhancement:**
1. Add distributed state management (Redis)
2. Implement comprehensive monitoring
3. Add retry logic for transient failures
4. Increase test coverage
5. Add rate limiting

---

**Document Version:** 1.0  
**Last Updated:** February 3, 2026
